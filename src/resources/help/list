Usage: moo list [STRING] [options]

  Options:
    -l, --local                      List packages installed locally
    -r, --remote                     List packages in all remote sources
    -s, --source NAME|URL            List packages in this specific source

  Arguments:
    STRING        start of package name to look for

  Summary:
    Display packages whose name starts with STRING

  Defaults:
    --local

